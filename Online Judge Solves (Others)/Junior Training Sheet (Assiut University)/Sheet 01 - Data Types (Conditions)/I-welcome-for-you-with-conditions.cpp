/*
    author: remonhasan
*/
#include<bits/stdc++.h>
#include<stdio.h>

using namespace std;

int main() {
  long long a, b;
  scanf("%lld %lld", & a, & b);
  if (a >= b)
    printf("Yes");
  else
    printf("No");
  return 0;
}